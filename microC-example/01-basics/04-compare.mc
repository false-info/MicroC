head(custom) {
    fn main() {
        I64 x = 3
        if (x >= 4) {
            pin("x is bigger then 4\n")
        }
        if (x <= 2) {
            pin("x is less then 2\n")
        }
        if (x == 3) {
            pin("x is 3\n")
        }
    }
}