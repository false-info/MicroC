head(custom) {
    fn add(I64 a, I64 b) {
        return a + b
    }

    fn sub(I64 a, I64 b) {
        return a - b
    }

    fn mult(I64 a, I64 b) {
        return a * b
    }

    fn main() {
        I64 add = add(10, 5)
        I64 sub = sub(10, 5)
        I64 mult = mult(10, 5)
    }

}